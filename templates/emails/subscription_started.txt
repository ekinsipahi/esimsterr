Your {{ subscription.target_name }} subscription is live.

Unlimited data, renewed every {{ subscription.interval_days }} days for ${{ subscription.price_usd }}.
Each renewal lands on the same eSIM, so there is nothing to reinstall.

Plan: {{ subscription.title }}
{% if subscription.current_period_end %}Next renewal: {{ subscription.current_period_end|date:"j M Y" }}{% endif %}
{% if esim %}ICCID: {{ esim.iccid }}{% endif %}

Open your subscription: {{ detail_url }}

Worth knowing:
1. Install the eSIM once. Every renewal tops up that same line.
2. The same amount is charged each period until you cancel, which you can do
   from your dashboard.
3. Turn Data Roaming ON for the eSIM line, or you get bars and no data.

-- {{ site_name }} · {{ support_email }}
